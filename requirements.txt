# Audio capture
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.26

# Transcription (CTranslate2 backend, no torch needed for whisper itself)

# Speaker diarization (needs torch)
pyannote.audio>=3.1
torch>=2.2
torchaudio>=2.2

# Тесты
pytest>=8.0

# Google Calendar
google-api-python-client>=2.100
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2


# Whisper на Apple GPU (в разы быстрее CPU)
mlx-whisper>=0.4
